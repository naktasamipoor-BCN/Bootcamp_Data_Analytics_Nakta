# Raw data note

This folder contains the small raw datasets used in the project.

The monthly Barcelona TRAMS raw CSV files are not included in this public repository because they are large files. The processed analysis outputs generated from those files are available in `data/outputs/`.

The Streamlit dashboard uses the processed output files, so the app can run without the large raw TRAMS CSV files.
